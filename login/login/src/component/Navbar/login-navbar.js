import React from "react";
import logo from "../icons/logo.png"

const LoginNavbar = () => {
    return(
        <nav className="login-nav">
        <div className="lms">
            <img className="img" src={logo} alt="logo" />
            
        </div>
        <component>
            <h1 className="logotext">Leave management system</h1>
        </component>
        </nav>
    )
}

export default LoginNavbar;